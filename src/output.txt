/* src/styles/typography.css */

:root {
  /* Font families */
  --font-body: "Inter", sans-serif;
  --font-heading: "Inter", sans-serif;

  /* Type scale */
  --text-xs: 0.8rem;
  --text-sm: 0.9rem;
  --text-base: 1rem;
  --text-lg: 1.25rem;
  --text-xl: 1.5rem;
  --text-2xl: 2rem;
  --text-3xl: 3rem;

  /* Line heights */
  --leading-tight: 1.2;
  --leading-normal: 1.6;
  --leading-loose: 1.8;

  /* Weights */
  --weight-regular: 400;
  --weight-medium: 500;
  --weight-bold: 700;
}

/* Default heading styles */
h1, h2, h3, h4 {
  font-family: var(--font-heading);
  font-weight: var(--weight-bold);
  line-height: var(--leading-tight);
}


/* src/styles/tokens.css */

:root {
  /* Colors */
  --color-bg: #0a0a0a;
  --color-surface: #1a1a1a;
  --color-border: #2a2a2a;
  --color-text: #f5f5f2;
  --color-text-muted: #b8b8b2;
  --color-accent: #d4a657;

  /* Spacing scale */
  --space-2: 0.125rem;
  --space-4: 0.25rem;
  --space-8: 0.5rem;
  --space-12: 0.75rem;
  --space-16: 1rem;
  --space-24: 1.5rem;
  --space-32: 2rem;
  --space-48: 3rem;
  --space-64: 4rem;

  /* Radius */
  --radius-sm: 6px;
  --radius-md: 12px;
  --radius-lg: 20px;

  /* Shadows */
  --shadow-sm: 0 2px 6px rgba(0, 0, 0, 0.2);
  --shadow-md: 0 4px 12px rgba(0, 0, 0, 0.25);

  /* Transitions */
  --transition-fast: 150ms ease;
  --transition-medium: 250ms ease;
}


/* src/styles/index.css */

/* Design system tokens */
@import "./tokens.css";

/* Typography scale + font families */
@import "./typography.css";

/* Layout primitives (Container, Grid, section spacing) */
@import "./layout.css";

/* Utility classes (flex, animations, helpers) */
@import "./utilities.css";

/* Global resets + base HTML defaults */
@import "./globals.css";


/* src/styles/layout.css */

/* Container */
.container {
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding-inline: var(--space-24);
}

/* Grid */
.grid {
  display: grid;
  gap: var(--space-32);
}

/* Section spacing */
section {
  padding-block: var(--space-64);
}


/* src/styles/utilities.css */

/* Flex helpers */
.flex {
  display: flex;
}
.flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}
.flex-between {
  display: flex;
  justify-content: space-between;
}

/* Text alignment */
.text-center {
  text-align: center;
}

/* Animation utilities */
.animate {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 600ms ease, transform 600ms ease;
}
.animate--visible {
  opacity: 1;
  transform: translateY(0);
}

/* Hide visually but keep accessible */
.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
}


/* src/styles/globals.css */

/* Reset */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Smooth text + rendering */
html {
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
}

body {
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-body);
  line-height: 1.6;
}

/* Remove default link styles */
a {
  color: inherit;
  text-decoration: none;
}

/* Images */
img {
  max-width: 100%;
  display: block;
}

/* Buttons */
button {
  font-family: inherit;
  cursor: pointer;
  border: none;
  background: none;
}


